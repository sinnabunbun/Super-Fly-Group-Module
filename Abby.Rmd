---
title: "Abby"
author: "Abby"
date: "11/5/2021"
output: html_document
---

install packages
```{r}
install.packages("expm")
install.packages("gee")
install.packages("igraph")
install.packages("ape")
```

Load packages 
```{r}
library(ggplot2)
library(Rcpp)
library(expm)
library(gee)
library(igraph)
library(ape)
```

```{r}
MyTree <- read.tree("BEAST.tre")
##Error in if (all(phy$node.label == "")) phy$node.label <- NULL : missing value where TRUE/FALSE needed
```


use rdryad to download tree directly from dryad into r 
```{r}
library(rdryad)
?rdryad
```

```{r}
install.packages("crul")
install.packages("curl")
install.packages("tibble")
install.packages("hoardr")
install.packages("zip")
install.packages("jsonlite")
install.packages("mime")
```

```{r}
library(crul)
library(curl) 
library(tibble)
library(hoardr)
library(zip) 
library(jsonlite) 
library(mime)
